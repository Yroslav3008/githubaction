name: Fiveth Workflow
on: [push, issues, pull_request ]

jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display Some text
        run: echo "Some text"
      - name: event Vars
        run: | 
          echo $GITHUB_SHA
          echo $GITHUB_REF