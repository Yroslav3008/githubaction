name: Docker Test
on: push

jobs:
  test_docker_container:
    runs-on: ubuntu-latest
    container: 
      image: node:12
      env:
        NODE_ENV: development
      ports:
        - 80
    steps:
      - name: Displey v
        run: node -v

