name: Seventh Workflow
on: 
  schedule:
    - cron: "*/2 * * * *"
jobs:
  run-first-command:
    runs-on: ubuntu-latest
    steps:
      - name: Display Some text
        run: echo "Some text"
      - name: event Vars
        run: | 
          echo $GITHUB_SHA
          echo $GITHUB_REF